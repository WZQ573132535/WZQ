version: '3.8'

services:
  codex-web:
    image: wenfxl/wenfxl-codex-manager:v1.0
    container_name: wenfxl_codex_manager
    ports:
      - "8090:8000"
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /volume1/docker/openai-cpa/config.yaml:/app/config.yaml
      - /volume1/docker/openai-cpa/data:/app/data