{
  "dns": {
    "servers": [
      { "tag": "local", "type": "udp", "server": "223.5.5.5" },
      {
        "tag": "public",
        "type": "https",
        "server": "dns.alidns.com",
        "domain_resolver": "local"
      },
      {
        "tag": "foreign",
        "type": "https",
        "server": "dns.google",
        "detour": "🚀 节点选择"
      },
      {
        "tag": "fakeip",
        "type": "fakeip",
        "inet4_range": "198.18.0.0/15",
        "inet6_range": "fc00::/18"
      }
    ],
    "rules": [
      { "clash_mode": "direct", "server": "local" },
      { "clash_mode": "global", "server": "fakeip" },

      { "query_type": "HTTPS", "action": "reject" },

      { "rule_set": "geosite-fakeipfilter", "server": "local" },
      { "rule_set": "geosite-cn", "server": "local" },
      {
        "rule_set": "geosite-!cn",
        "server": "fakeip",
        "rewrite_ttl": 1
      }
    ],
    "final": "foreign",
    "strategy": "prefer_ipv4",
    "independent_cache": true,
    "reverse_mapping": true
  },

  "outbounds": [
    {
      "tag": "🚀 节点选择",
      "type": "selector",
      "outbounds": [
        "♻️ 自动选择",
        "🇭🇰 香港自动",
        "🇺🇸 美国自动",
        "🇯🇵 日本自动",
        "🇨🇳 台湾自动",
        "🇸🇬 新加坡自动",
        "🇰🇷 韩国自动"
      ]
    },

    { "tag": "📹 YouTube", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "🔍 Google", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "💬 OpenAi", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "❎ X", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "✨ Gemini", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "🎵 TikTok", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "🌈 Instagram", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "📲 Telegram", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "🎮 Nintendo", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "👨🏿‍💻 GitHub", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "👾 Docker", "type": "selector", "outbounds": ["🚀 节点选择"] },
    { "tag": "🎥 Netflix", "type": "selector", "outbounds": ["🚀 节点选择"] },

    { "tag": "🍎 Apple", "type": "selector", "outbounds": ["🎯 全球直连"] },
    {
      "tag": "🐟 漏网之鱼",
      "type": "selector",
      "outbounds": ["🚀 节点选择"]
    },

    { "tag": "🇭🇰 香港自动", "type": "urltest", "outbounds": [] },
    { "tag": "🇺🇸 美国自动", "type": "urltest", "outbounds": [] },
    { "tag": "🇯🇵 日本自动", "type": "urltest", "outbounds": [] },
    { "tag": "🇨🇳 台湾自动", "type": "urltest", "outbounds": [] },
    { "tag": "🇸🇬 新加坡自动", "type": "urltest", "outbounds": [] },
    { "tag": "🇰🇷 韩国自动", "type": "urltest", "outbounds": [] },
    { "tag": "♻️ 自动选择", "type": "urltest", "outbounds": [] },

    {
      "tag": "GLOBAL",
      "type": "selector",
      "outbounds": ["🚀 节点选择", "🎯 全球直连"]
    },

    { "tag": "🎯 全球直连", "type": "direct" }
  ],

  "route": {
    "rules": [
      {
        "action": "sniff",
        "sniffer": ["http", "tls", "quic", "dns"]
      },
      { "inbound": "dns-in", "action": "hijack-dns" },
      { "ip_is_private": true, "outbound": "🎯 全球直连" },

      { "clash_mode": "direct", "outbound": "🎯 全球直连" },
      { "clash_mode": "global", "outbound": "GLOBAL" },

      { "rule_set": "geosite-openai", "outbound": "💬 OpenAi" },
      { "rule_set": "geosite-x", "outbound": "❎ X" },
      { "rule_set": "geosite-gemini", "outbound": "✨ Gemini" },
      { "rule_set": "geosite-youtube", "outbound": "📹 YouTube" },
      { "rule_set": "geosite-google", "outbound": "🔍 Google" },
      { "rule_set": "geosite-github", "outbound": "👨🏿‍💻 GitHub" },
      { "rule_set": "geosite-docker", "outbound": "👾 Docker" },
      { "rule_set": "geosite-telegram", "outbound": "📲 Telegram" },
      { "rule_set": "geosite-tiktok", "outbound": "🎵 TikTok" },
      { "rule_set": "geosite-instagram", "outbound": "🌈 Instagram" },
      { "rule_set": "geosite-nintendo", "outbound": "🎮 Nintendo" },
      { "rule_set": "geosite-netflix", "outbound": "🎥 Netflix" },
      { "rule_set": "geosite-apple", "outbound": "🍎 Apple" },

      { "rule_set": "geosite-!cn", "outbound": "🚀 节点选择" },
      { "rule_set": "geosite-cn", "outbound": "🎯 全球直连" },

      { "rule_set": "geoip-cn", "outbound": "🎯 全球直连" }
    ],
    "final": "🐟 漏网之鱼"
  },

  "inbounds": [
    {
      "tag": "dns-in",
      "type": "direct",
      "listen": "::",
      "listen_port": 1053
    },
    {
      "tag": "http-in",
      "type": "http",
      "listen": "::",
      "listen_port": 8080
    },
    {
      "tag": "socks-in",
      "type": "socks",
      "listen": "::",
      "listen_port": 7891
    },
    {
      "tag": "redirect-in",
      "type": "redirect",
      "listen": "::",
      "listen_port": 7890
    },
    {
      "tag": "tproxy-in",
      "type": "tproxy",
      "listen": "::",
      "listen_port": 1080
    },
    {
      "tag": "tun-in",
      "type": "tun",
      "interface_name": "momo",
      "address": [
        "172.31.0.1/30",
        "fdfe:dcba:9876::1/126"
      ],
      "auto_route": false,
      "auto_redirect": false
    }
  ],

  "experimental": {
    "cache_file": {
      "enabled": true,
      "path": "/etc/momo/run/cache.db",
      "store_fakeip": true
    },
    "clash_api": {
      "external_controller": "0.0.0.0:9095",
      "external_ui": "/etc/momo/run/ui",
      "external_ui_download_url": "https://github.573132535.xyz/https://github.com/Zephyruso/zashboard/archive/refs/heads/gh-pages.zip",
      "external_ui_download_detour": "🎯 全球直连",
      "default_mode": "rule"
    }
  },

  "log": {
    "disabled": false,
    "level": "info",
    "timestamp": true
  }
}
